// Shared values live here so the client and server agree on basic project settings.
export const PROJECT_NAME = "MuMoo.io";

export const NETWORK_CONFIG = {
  defaultPort: 3001,
  websocketPath: "/ws"
} as const;
